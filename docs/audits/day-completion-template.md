# Day N 完成档案（YYYY-MM-DD）

> 使用时复制本文件并命名为 `dayN-completion-YYYY-MM-DD.md`。删除本提示以及所有占位文字；不得预先为空闲工作包创建“完成”档案。

## 原计划与范围

- 原计划：链接到 `docs/planning/DayN.md`。
- 前置 commit：`<hash>`。
- 本次范围：`<实际进入本工作包的内容>`。
- 明确非目标：`<未授权或属于后续 Day 的内容>`。

## 逐项完成情况

| 原计划项 | 状态 | 代码/文档证据 | 说明 |
|---|---|---|---|
| 1 | `completed / partial / not-run / blocked` | `<路径、测试或 hash>` | `<事实说明>` |

不得删除未完成项。`partial`、`not-run` 和 `blocked` 必须写明原因、风险和唯一后续主责工作包。

## 关键实现与安全边界

- `<关键实现>`
- `<失败关闭、权限、数据或架构边界>`
- `<保持兼容且未扩大的能力>`

## 验证证据

| 命令或检查 | 退出码 | 结果 |
|---|---|---|
| `<pnpm typecheck / test / build / smoke / benchmark 等>` | `<0/非 0>` | `<文件数、测试数、指标或失败原因>` |

## 可复现标识

- 完成 commit：`<hash；提交前写“待提交”>`。
- 定义、数据库或产物 hash：`<适用时填写>`。
- Fixture/Suite/Schema/Prompt 版本：`<适用时填写>`。
- 生成物目录及 Git 忽略状态：`<路径和检查证据>`。

## 残余风险与后续依赖

- `<Inconclusive、未运行或剩余风险>`
- `<后续 Day、负责人或解除条件>`

## 能力声明边界

本档案只证明 `<准确、有限的交付范围>`；不证明 `<不得外推的真实目标、协议、漏洞类别、资格或性能结论>`。
