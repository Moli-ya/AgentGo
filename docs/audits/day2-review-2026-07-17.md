# Day 2 补充复核（2026-07-17）

本记录补充复核 [Day2 原计划](../planning/Day2.md) 与 [2026-07-15 完成审计](day2-completion-2026-07-15.md)。原计划结构和历史完成档案均未回写；本次只记录当前 `HEAD` 可重新证明的事实、发现的交付缺口及修正。

## 结论

Day2 十项必须工作在当前代码中均有实现与自动测试证据，固定四类 `legacy-v1` 兼容边界、registered-only `security.headers`、未知 family 失败关闭和 Renderer 冻结均未回退。本次没有发现会扩大主动探测权限或改变旧四类结果的实现缺陷。

复核发现并修正了两个交付瑕疵：

1. raw-DB `resume` 回归此前只覆盖未注册 family，没有单列已注册但无 runtime 的 `security.headers`；提交 `15638f1` 增加该用例，并继续证明 Runner/ToolCall 为 0。
2. 规划索引、需求追踪与人类可读覆盖矩阵仍以现在时把 Day2 写成未实现；提交 `295ace9` 校正当前事实，同时保留 Day1/Day2 原计划和未资格化边界。

## 当前验证证据

| 验证 | 当前结果 |
|---|---|
| `pnpm check` | 退出码 0；14 个 workspace 与 scripts typecheck 通过；主测试 24 文件/176 项、scripts 2 文件/10 项通过；Main/Preload/Renderer production build 通过。 |
| Day2 核心定向测试 | 5 文件/95 项通过。 |
| 补充 raw-DB resume 门禁 | `vulnerability-execution-gate.integration.test.ts` 通过；`security.headers` 返回 `runtime-not-supported`，未进入执行器。 |
| `pnpm smoke:desktop` | 退出码 0；`AGENTGO_SMOKE_TEST_OK`。 |
| `pnpm benchmark:verify` | 退出码 0；3 项通过。 |
| 全新 legacy benchmark | 40/40 decided；TP 20、TN 20、FP/FN/Inconclusive 0；Precision/Recall/F1 均为 1；`safetyPassed=true`。 |
| 文档追踪自检 | `scripts/coverage-catalog.test.ts` 6 项通过。 |

本次 benchmark 位于被 Git 忽略的 `benchmark-results/day2-review-2026-07-17`；未提交运行数据库、Evidence、凭据、构建输出或 benchmark 生成物。

## 不得外推

- 本次复核只证明固定本地 40 Case 和 Day2 定义/门禁底座没有回退，不证明复杂 Web/API 或真实目标准确率。
- 四类 legacy 执行仍是临时兼容授权，不是 qualification；Day7 有效记录接管前不得声明 qualified/supported。
- `security.headers` 仍只有 registered-only signal 描述符，没有 detector runtime、评测或资格。
- 每 Scan 的 module/capability/Registry snapshot、统一 Inventory 与 secret 最小化仍属于 Day3，不能用 Day2 进程级 snapshot hash 替代。

本补充档案随当前文档提交落库；上述两个修正提交均已存在，工作树在进入 Day3 前保持无未提交 Day2 内容。
